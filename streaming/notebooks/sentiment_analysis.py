# Databricks notebook source
# Streaming sentiment (placeholder)
# reviews = spark.readStream.table("retail.bronze.reviews")
# TODO: parse payload, join with product dim, simple sentiment
# enriched.writeStream.format("delta").outputMode("append").option("checkpointLocation","...").table("retail.silver.reviews_enriched")
